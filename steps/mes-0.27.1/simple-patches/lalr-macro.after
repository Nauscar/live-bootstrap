  (define-macro (wrap-term term)
    `(cons (quote ,(if (symbol? term)
                       (if (reserved? term)
                           'terminal
                           'non-terminal)
                       'terminal))
           (quote ,term))))
